#!/usr/bin/python3
def multiple_returns(sentence):
    if len(sentence) > 0:
        f_letter = sentence[0]
        length = len(sentence)
        return f_letter, length
    else:
        f_letter = None
        return f_letter
