#!/usr/bin/python3
import sys
if len(sys.argv) > 1:
    print("{} arguments:".format(len(sys.argv)))
    for i in range(len(sys.argv)):
        if i > 0:
            print("{}: {}".format(i + 1, sys.argv[i]))
else:
    print("0 arguments.")
